import { style } from '@openuxkit/styles/radiobuttongroup';
import BaseStyle from '@openvue/core/base/style';

const classes = {
    root: 'p-radiobutton-group p-component'
};

export default BaseStyle.extend({
    name: 'radiobuttongroup',
    style,
    classes
});
