import { style } from '@openuxkit/styles/iftalabel';
import BaseStyle from '@openvue/core/base/style';

const classes = {
    root: 'p-iftalabel'
};

export default BaseStyle.extend({
    name: 'iftalabel',
    style,
    classes
});
